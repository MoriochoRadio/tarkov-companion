# Tarkov Companion — 설계서

> 작성: 2026-06-11 · 상태: 초안 v1

## 1. 한 줄 정의

Escape From Tarkov 플레이어를 위한 **AI 큐레이션 컴패니언 웹** — 실시간 플리마켓 시세·가성비 분석 대시보드 + 매일 자동 생성되는 한국어 브리핑(패치/커뮤니티 꿀팁/메타 변화).

## 2. 차별화 (기존 tarkov.dev, tarkov-market과 다른 점)

1. **AI 일일 브리핑** — 패치노트 + 커뮤니티 정보를 매일 AI가 요약 배달 (기존 사이트 없음)
2. **해석하는 시세** — 단순 나열이 아닌 "오늘 슬롯당 가치 톱", "급등/급락 경보" 등 분석 제공
3. **한국어 우선** — 기존 도구는 전부 영어 중심

## 3. 아키텍처

```
[방문자 브라우저] ──직접 호출──> api.tarkov.dev/graphql (무료, 키 불필요)
       │
       └─ GitHub Pages (정적 호스팅, 무료)
              ▲
              │ 커밋 → 배포 dispatch (매일 09:00 KST)
[GitHub Actions daily-briefing] ── 수집 → GitHub Models 요약 → public/data/briefings/YYYY-MM-DD.json
```

- 서버 없음. 시세는 방문자 브라우저가 API를 직접 호출 → 운영비 0원
- 브리핑은 GitHub Actions가 매일 생성해 리포에 커밋 → 배포 워크플로우를 dispatch해 사이트에 게시 (GitHub 안에서 완결, 구독·유료 의존 없음)
- 브리핑 소스: EFT 위키 체인지로그(MediaWiki API, 공식 패치노트 수록) + Reddit r/EscapefromTarkov 일간 인기글(RSS). 공식 뉴스 페이지는 JS 렌더링 SPA + 내부 API 403이라 직접 수집 불가(2026-06-11 확인) → 위키 체인지로그로 대체. 디시/인벤은 약관 이슈로 보류

## 4. 기술 스택 (전부 무료)

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | React + TypeScript + Vite | 표준 스택, 유지보수 용이 |
| 데이터 | tarkov.dev GraphQL API | 무료·키 불필요·한국어 지원(lang: ko) |
| 호스팅 | GitHub Pages | 무료, 리포 push만으로 배포 |
| CI/CD | GitHub Actions | push 시 자동 빌드·배포 (무료) |
| 브리핑 생성 | GitHub Actions + GitHub Models | GITHUB_TOKEN만으로 무료 AI 요약, 리포 안에서 완결 |

## 5. 로드맵

### Phase 1 — MVP 대시보드 (여기서 시작)
- [ ] 아이템 검색 (한국어명 지원)
- [ ] 가성비 랭킹: 슬롯당 플리마켓 가치 톱 N
- [ ] 급등/급락: changeLast48hPercent 기준 상위/하위
- [ ] 탄약 비교 테이블 (데미지/관통/가격)
- [ ] GitHub Pages 배포

### Phase 2 — 일일 브리핑 (완료)
- [x] 브리핑 JSON 스키마 확정 (`docs/briefing-schema.md`)
- [x] 자동화 파이프라인: `.github/workflows/daily-briefing.yml` (매일 00:00 UTC = 09:00 KST + 수동 실행)
  - `scripts/collect-briefing.mjs` — 위키 체인지로그 + Reddit RSS 수집 (소스별 독립 실패 허용)
  - `scripts/generate-briefing.mjs` — GitHub Models(openai/gpt-4.1-mini)로 한국어 요약, 실패 시 링크 목록 폴백 (빈 날 없음)
  - 커밋 후 배포 워크플로우 dispatch (GITHUB_TOKEN push는 push 트리거를 발화시키지 않으므로 필수)
- [x] 사이트에 "오늘의 브리핑" 탭 (기본 탭)

### Phase 3 — 다듬기
- [x] PWA (폰 홈 화면 추가) — Phase 8에서 완료
- [x] 시세 히스토리 차트 — Phase 8에서 스파크라인으로 완료
- [ ] 영어 지원 (해외 공유용)

### Phase 4 — 브리핑 파이프라인 강화 (완료)
- [x] 수집 소스 확장: Reddit 플레어 검색 RSS(버그·이슈 등 전용 피드), YouTube 채널 RSS(노잼망겜·유우양·Pestily·LVNDMARK, 최근 24h), Steam 뉴스 RSS(appid 3932890)
- [x] AI 2패스: 1차 "기자"(소스 그룹별 요약) → 2차 "편집장"(통합·중복 제거·중요도 랭킹). 어제 브리핑 대비 새 이슈에 `isNew` 표시
- [x] 비용 안전장치: 호출 횟수 로깅 + 프로세스당 상한 20회 (`scripts/github-models.mjs`). 평소 하루 ≤5회 — GitHub Models 무료 한도(50회/일)의 절반 이하 유지
- [x] 주간 메타 리포트: 매주 월요일 01:00 UTC `weekly-report.yml`이 지난 7일 브리핑 종합 → `public/data/weekly/`, 브리핑 탭에서 선택 조회
- [x] 스키마 v2: sections type `videos` 추가, items `isNew` 추가 (`docs/briefing-schema.md`)

### Phase 5 — 퀘스트 탭 (완료)
- [x] tarkov.dev `tasks` 쿼리 기반 퀘스트 브라우저 (510개, AI 불사용)
  - ko/en 두 벌을 별칭으로 한 요청에 받아 "한국어명 (English)" 병기
  - 목록: 트레이더/맵/내 레벨 필터, 한/영 검색, 레벨순/트레이더순 정렬, 60개 단위 점진 렌더링
  - 상세: 목표, 필요 아이템(FIR 표시), 보상, 선행/후행 퀘스트 이동, 카파 뱃지, 위키 링크
  - 응답 ~3MB → 탭 첫 진입 시 지연 로드 후 세션 캐시
- [x] 브리핑 영상 섹션 품질: 채널당 최대 2개, Shorts 제외(휴리스틱), 무의미한 AI 요약 금지(영상은 제목+채널만 — 스키마에서 summary 선택화)

### Phase 6 — UI/UX 전면 개편 (완료)
- [x] CSS 변수 디자인 토큰: 그래파이트 + 탄 골드 단일 액센트, 숫자 모노스페이스(tabular-nums), Pretendard + JetBrains Mono
- [x] 모바일 전면 대응 (375px 오버플로 0, 탄약·퀘스트 카드형 전환), OG·파비콘, README 스크린샷

### Phase 7 — 퀘스트 공략 강화 + 맵 허브 (완료)
- [x] 퀘스트 상세: 아이템 아이콘 칩 + 512px 라이트박스, tarkov.dev 맵 딥링크, YouTube 공략 검색 링크
- [x] "맵" 탭: maps 쿼리 카드(레이드/인원/보스/요구 키) + 외부 링크. 한글 지도는 `public/data/map-links.json`으로 링크만 관리 (이미지 미수록)
- [x] 위키 가이드 AI 백필: `quest-guides.yml` 매일 02:00 UTC 30개씩 (호출 상한 30/일, 전체 ~2-3주). 위키 Guide 섹션 → 한국어 단계별 요약 → `public/data/guides/` (CC BY-SA 출처 표기, 스키마: `docs/quest-guide-schema.md`)

### Phase 8 — UX 폴리시 + 편의 기능 (완료)
- [x] PWA: manifest + 서비스워커 — 홈 화면 추가 시 standalone 실행, 앱 셸·해시 번들·마지막 브리핑 오프라인 캐시 (시세 API는 POST라 항상 실시간). 아이콘은 `scripts/make-icons.mjs`로 파비콘에서 생성
- [x] 즐겨찾기: 아이템 ★(검색/가성비, localStorage) + 검색창 비었을 때 모아보기, 퀘스트 "진행 중" 표시 + 필터 (내 레벨 필터와 조합해 "지금 할 일" 뷰)
- [x] 시세 스파크라인: `historicalItemPrices(days: 7)`를 GraphQL 별칭으로 배치 조회(최대 30개/요청, 아이템별 캐시). 아이템당 별도 조회라 무거워서 즐겨찾기에만 표시, 인라인 SVG
- [x] 마이크로 인터랙션: 로딩 스켈레톤(텍스트 안내 대체), 탭/패널 페이드(160ms), 호버 트랜지션(120ms) — 전부 200ms 이하, reduced-motion 존중
- [x] 모바일 탭바 우측 페이드+화살표 힌트, 브리핑 날짜 ◀▶ 네비게이션

### Phase 9 — "쇼룸" 첫인상 (완료)
- [x] 풀스크린 히어로 인트로 (첫 방문 전용): canvas 2D 재/연기 입자 + 레이더 스윕 그리드 (`src/lib/heroCanvas.ts` — WebGL 안개는 저사양 컨텍스트 손실 처리 부담 대비 이득 없어 2D 채택, 4x 스로틀에서 60fps 실측). 로고 스캔 와이프+글리치 1회+골드 라인, 라이브 지표 3종 카운트업(id만 받는 경량 쿼리), 스크롤/버튼/키 입장
- [x] 게이트: `tc:visited`(localStorage) + PWA standalone 감지 → 재방문·설치 실행은 히어로 스킵 (매일 쓰는 사람 방해 금지). reduced-motion은 정적 배경+리빌 생략+즉시 입장
- [x] 도구 마이크로 디테일: 탭 인디케이터 슬라이딩, 맵 카드 3D 틸트(±2.2°, rAF), 급등/급락 신규 항목 골드 펄스 1회(직전 방문 대비), 스파크라인 그라데이션+호버 툴팁, 즐겨찾기 시세 카운트업(세션 1회)
- [x] 히어로 성능 측정은 `scripts/profile-hero.mjs` (profile-ui.mjs는 visited를 심어 히어로를 건너뜀). OG 이미지·README 스크린샷을 히어로 룩으로 갱신

### Phase 10 — "상황실 + 무기고" (완료)
- [x] 파트 A 상황실: 입자+레이더 캔버스를 대시보드 상시 배경으로(ambient 모드 — 입자 절반, opacity 13%), 헤더 아래 급등/급락 티커(호버 정지, 클릭→검색, 1.5초 지연 마운트), 브리핑 상황실 그리드(타이프라이터 헤드라인 1회, warning/영상 와이드 + 2열, 유튜브 썸네일 카드, stagger ≤0.5초), 데스크톱 한정 backdrop-blur 유리 카드
- [x] 파트 B 무기고: three.js 3D 무기 뷰어 — Quaternius CC0 모델 2정(134KB, `public/models/LICENSE.md`), 골드 림라이트, 자동 회전+드래그, 페이드 무기 전환. 히어로 로고 아래 + 대시보드 헤더 위젯 → 풀스크린 뷰어. three는 dynamic import 별도 청크(gz 153KB, 첫 페인트 영향 gz +1.5KB), 모바일·저사양·WebGL 불가·컨텍스트 손실은 사전 렌더 포스터 폴백(`scripts/make-weapon-posters.mjs`)
- [x] 4x 스로틀 실측: 히어로(3D 포함) 60fps·최대 롱태스크 866ms, 도구 탭 최대 729ms — 1초 블로킹 금지 통과. 프로파일 스크립트에 `--use-gl=angle` 추가(헤드리스 WebGL)

### Phase 11 — 디자인 문법 대격변: "인터랙티브 매거진" (완료)
- [x] 타이포 대격변: 탭마다 화면급 마스트헤드(모노 눈썹 라벨 + Pretendard Black 900 초대형 타이틀, 브리핑은 골드 모노 날짜) — 본문 0.92rem 대비 6배 이상. 맵 이름·퀘스트 타이틀도 디스플레이급(`--fs-display`, `--fs-display-2` 토큰)
- [x] 장면 전환: View Transitions API로 본문 패널만 좌→우 와이프+페이드 (root 전환은 꺼서 헤더·배경 유지, 미지원 브라우저는 기존 panel-in 폴백). 탭 전환 순간 배경 캔버스 펄스 1회(`heroCanvas` pulse API — 골드 링 확산 + 입자 가속 후 감쇠) + 커서 패럴랙스(±3px transform, pointer:fine 한정)
- [x] 벤토 그리드 + 비주얼 (이미지는 전부 tarkov.dev + 유튜브 썸네일): 브리핑 6열 차등 벤토(lg 4칸/sm 2칸/wide 전체), 영상 카드 2배 크기 풀블리드+오버레이, 글 항목 영상 링크 미니 썸네일. 맵 카드 보스 초상화 배너(`imagePortraitLink`) + 초대형 맵 이름. 퀘스트 목록 트레이더 초상화(`trader.imageLink`) + 상세 빅 헤더
- [x] ~~질감: 필름 그레인 + 전역 비네트~~ — **사용자 피드백으로 제거됨** (화면이 뿌옇게 가려져 가독성 저하. 약하게 조정해도 어두운 테마 위에서는 손해만 있음 — 재도입 금지)
- [x] 정리: 헤더 3D 무기 위젯·풀스크린 뷰어 제거(3D는 히어로 전용), 텍스트/보더 대비 상향, 골드 빈도 증가(섹션 제목 틱·마스트헤드·탭 호버)
- [x] 4x 스로틀 실측: 도구 탭 최대 롱태스크 790ms, 히어로 792ms·120fps — 1초 블로킹 금지 통과. 스크린샷 점검은 `scripts/shoot-ui.mjs`

### Phase 12 — 친구 피드백: 준비물 체크리스트 + 플리 수수료 + 모딩 브라우저 (완료)
- [x] 파트 A "준비물" 탭: 퀘스트 제출(giveItem/plantItem) + 은신처(hideoutStations) 요구를 아이템별 통합 집계 — "레이드에서 버리거나 팔면 안 되는 것" 목록. 아이콘+한/영 병기, FIR/퀘스트/은신처 분해 표시, 펼치면 출처 상세. +/− 체크리스트(`tc:prep-counts`)와 진행률, 다 모은 아이템 접기, 검색·수량순 정렬
  - 집계 제외 규칙: findItem(giveItem과 짝이라 이중 계산), 선택형 다중 아이템 목표, 화폐(id 직접 차단 — types로 구분 불가, Buyout류 440만 루블 도배 실측)
  - "내 레벨"은 `lib/playerLevel.ts`로 분리해 퀘스트 탭과 공유 + localStorage 영속화. "진행 중 퀘스트만"은 기존 ★ 표시와 연동
- [x] 파트 B 플리 수수료: 위키 Trading#Tax 공식 `lib/fleaFee.ts` — **1.0에서 세율 Ti=Tr=0.03으로 변경 확인**(과거 0.05/0.1). `scripts/check-flea-fee.mjs`가 API의 fleaMarketFee(서버 계산)와 대조해 20케이스 정확 일치. 실시간 세율은 아이템 쿼리에 fleaMarket 필드를 끼워 받음(추가 요청 0). 검색/즐겨찾기 "실수익" 칼럼, 가성비 랭킹 "수수료 제외 실수익 기준" 토글, 접이식 수수료 계산기(정보센터 3레벨 할인)
- [x] 파트 C "모딩" 탭: 무기 선택 → 슬롯별 호환 부품. 전체 트리 대신 보는 아이템 단위 lazy 조회+캐시(M4A1 1단계 ~63KB 실측). 무기·모드 속성 타입이 slots 필드를 공유해 같은 쿼리로 하위 슬롯 드릴다운(리시버→총열·조준경, 빵부스러기 복귀). 트레이더 레벨(1~4) 필터(현금 오퍼만, ⚿=퀘스트 해금), 에르고/반동/가격 정렬. 풀 빌드는 범위 외 — tarkov.dev 빌더 딥링크
- [x] 4x 스로틀 실측(탭 9개로 늘어난 뒤): 최대 롱태스크 789ms(퀘스트 탭 첫 렌더, 기존과 동일) — 1초 블로킹 금지 통과. 준비물/모딩 진입 rAF 응답 1~5ms. 모바일 375px 가로 오버플로 0, 탭바 우측 스크롤 힌트 동작 재확인. profile-ui.mjs에 새 탭 5개 시나리오 추가

### Phase 13 — 모딩 탭 개편: "부품 백과" → "추천 빌드 총정리" (완료)
- [x] 친구 피드백 반영: 부품 하나하나 탐색은 쓰기 어려움 → 기본 화면을 **레벨별 추천 빌드 카드 그리드**로 교체. 기존 슬롯 드릴다운 브라우저는 "부품 직접 탐색" 토글로 강등 (삭제 안 함)
- [x] 시드 데이터 `public/data/builds.json`: 16개 빌드 — 돌격소총 4(LL1~4)·SMG 4·DMR 3·샷건 3·저격 2. 부품은 전부 `scripts/explore-weapon.mjs`(저작 도구, 무기 슬롯 트리를 티어 필터로 덤프)의 실측 데이터에서 선정. 형식 문서 `docs/builds-schema.md` — 저장소 주인이 직접 추가·수정 가능
- [x] `scripts/validate-builds.mjs`: 모든 부품의 장착 가능 여부를 API slots로 자동 검증. 판정은 연쇄 허용 — 무기 슬롯 / 빌드 내 다른 부품 슬롯(마운트→도트) / **무기 기본 프리셋 부품의 슬롯**(기본 핸드가드→포어그립). 티어 내 현금 구매 불가·퀘스트 해금은 경고로 표시. 시드 16개 전부 통과 확인 후 커밋 (첫 검증에서 SR-25 소염기 1건 장착 불가 적발 → 제거)
- [x] 카드 UI: 512px 무기 이미지 배너 + LL 티어·카테고리 뱃지 + 부품 아이콘 스트립 + 실시간 시세 합산 총비용(티어 내 트레이더 현금가 우선, 없으면 플리 폴백) + 최종 에르고/수직반동(부품 보정 합산 — 근사치 명시) + 한 줄 설명. 펼치면 부품별 구매처·가격. 필터: 카테고리 / "내 트레이더 LL{n} 이하" 즉답 구조. 카드 데이터는 빌드 등장 아이템만 ids로 받는 단일 쿼리(수십 KB)
- [x] 4x 스로틀 실측: 최대 롱태스크 551ms, 빌드 카드 진입 rAF 8ms — 1초 기준 통과. profile-ui.mjs 시나리오를 새 기본 화면(카드→펼침→토글→브라우저)으로 갱신

### Phase 14 — 피드백 반영: 모딩 정보 밀도 + 준비물 "인게임 은신처" 개편 (완료)
- [x] 모딩 빌드 카드 품질 (피드백: "베이스 모델 이미지라 이상함, 쓸 이유가 없음"):
  - 배너를 **기본 프리셋(조립 상태) 이미지**로 교체 (`defaultPreset.image512pxLink`) + 구경 라벨. 부품 구성과 다를 수 있음은 펼침 하단에 명시
  - 스탯 강화: 에르고·수직반동 **전→후 델타**(기본 31 → 45식, 좋아진 쪽 골드) + 인게임풍 스탯 바, 펼치면 수평반동·연사력·총 무게(부품 합산), 부품별 에르고/반동 보정 칩
  - **추천 탄약**: 빌드 구경의 관통력 톱 3 (관통/데미지/시세) — 탄약 탭과 캐시 공유, 펼친 카드에서만 로드
- [x] 준비물 3뷰 개편 (피드백: "인게임 하이드아웃처럼, 카테고리·미션 세분화"):
  - **은신처 뷰**: 스테이션 26개 아이콘 카드 그리드(인게임풍, 골드 틴트) → 선택하면 레벨별 요구 아이템(화폐 포함 표시)·선행 스테이션/트레이더/스킬·건설 시간. **"지었음" 체크**(localStorage `tc:hideout-built`, 인게임처럼 하위 레벨 연쇄) → 통합 체크리스트 집계에서 그 레벨 몫 자동 제외
  - **퀘스트 뷰**: 트레이더 초상화 그룹 → 퀘스트별 제출 아이템(FIR 뱃지·수량), 내 레벨/FIR만/진행 중 필터, ★ 연동. 그룹은 펼친 것만 렌더(300퀘스트 DOM 폭증 방지)
  - 기존 통합 체크리스트는 기본 뷰 유지(레이드 중 시나리오) — 모드 세그먼트로 전환
- [x] 4x 스로틀 실측: 최대 롱태스크 558ms — 1초 기준 통과. 모바일 375px 오버플로 0

### Phase 15 — 인터랙션 폴리시: "조용하지만 살아있는" (완료)
- 방향: 2026 트렌드 리서치(커서 근접 반응·스크롤 내러티브·"visual theatrics의 종말" = 절제) 기준으로, 매거진 문법 위에 마이크로 인터랙션만 얹음. 그레인·비네트 금지 유지. 전부 transform/opacity + 환경 게이트(pointer:fine, reduced-motion)
- [x] 파트 A 커서 스포트라이트: 카드 9종에 커서 추적 radial 글로우 — 좌표만 JS(문서 위임 리스너 1개 + rAF)가 CSS 변수로 공급, 그리기는 CSS. ★ 토글 스프링 팝, +/− 프레스, ::selection·스크롤바·focus-visible 골드 통일
- [x] 파트 B 키네틱 마스트헤드: 탭 전환마다 글자 단위 스태거 등장(30ms, key 리마운트) + 마침표 팝 + 눈썹 라인 성장. 첫 로드 헤더→티커→탭바 오케스트레이션. **주의: Masthead key를 탭 키 그대로 쓰면 형제 ActiveComp와 key 중복 → React 재조정 붕괴(옛 마스트헤드 DOM 잔류). 접두사 필수**
- [x] 파트 C 스크롤 연동: 목록 카드 리빌 `animation-timeline: view()` + 진행 바 `scroll()` (Chrome 115+ 점진 향상, JS 0). 리빌 키프레임은 from만 정의 — fill로 다른 transform(틸트)을 덮지 않게. 탭바 sticky 유리 바(데스크톱 블러, 모바일 불투명)
- [x] 4x 스로틀 최대 롱태스크 576ms — 기준 통과. (참고: Puppeteer screenshot clip은 뷰포트가 아니라 문서 좌표 — sticky 검증은 getBoundingClientRect 수치로 할 것)

### Phase 16 — 커뮤니티 요청 기능: 탄약 매트릭스 + "돈벌이" (완료)
- 선정 기준: 타르코프 커뮤니티 상시 요청 기능 중 무료 데이터(tarkov.dev)로 구현 가능한 것 — ① 색상 탄약 차트(방어구 효율), ② 크래프트/바터 수익 계산기. (고려 후 보류: 보스 트래커·와이프 타이머는 신뢰할 무료 데이터 소스 없음)
- [x] 탄약 차트: C2~C6 방어구 클래스별 관통 효율 0~6 색칩(적→녹) — 근사식 `clamp(round((pen−(c×10−15))/5), 0, 6)`, 근사임을 범례 명시. 데미지·관통 미니 막대, 표 공통 지브라
- [x] "돈벌이" 탭 (PROFIT LAB): 크래프트 211·바터 787의 실시간 수익 랭킹
  - 수익 = 산출물 플리 실수익(Phase 12 수수료 공식 재사용) − 재료 플리 시세. 크래프트는 시간당 환산
  - 도구(attributes type=tool)는 비용 제외(🔧), 시세 없는 재료 낀 레시피는 순위 왜곡 방지 위해 제외
  - **"내 은신처에서 가능만"** — Phase 14 은신처 "지었음" 체크(`tc:hideout-built`)와 연동. 레시피는 id+개수만 받아 아이템 캐시와 조인(수십 KB)
- [x] 4x 스로틀 최대 롱태스크 674ms — 기준 통과 (profile-ui에 돈벌이 시나리오 추가)

### Phase 17 — 빠른 검색 + 맵·열쇠 정보 강화 (완료)
- [x] **Ctrl+K 커맨드 팔레트**: 어느 탭에서든 탭 이동·아이템·퀘스트 검색/점프 (헤더 버튼 병행). 아이템→검색 탭 시드(티커 패턴 재사용), 퀘스트→상세 직행(`setPendingQuest` + questNonce 리마운트). 세션 캐시 재사용이라 추가 트래픽 없음. ↑↓/Enter/Esc·listbox 시맨틱
- [x] 맵 탭: 접이식 탈출구 목록 — PMC 전용/공용/스캐브 전용 진영 그룹, "PMC 이용 가능 N" 요약
- [x] 돈벌이 탭 3번째 모드 "열쇠 가성비": 회당 비용(시세÷사용 횟수) 정렬 + 🗝 퀘스트 필요 뱃지(usedInTasks) + 퀘스트 열쇠만 필터. 열쇠 전종 별도 경량 쿼리
- [x] 성능 회귀 수정: 돈벌이 탭 첫 렌더 50행(행당 아이콘 다수)이 4x에서 1081ms 단일 태스크로 측정 → 2단계 렌더(14→50행) 적용, 최대 롱태스크 452ms로 복구. **교훈: 아이콘 많은 목록은 무조건 2단계 렌더로 시작할 것**

### Phase 18 — 가격 알림 + 내 데이터 백업 (완료)
- [x] **가격 알림** (즐겨찾기 🔔): 목표가(이상/이하) 설정 → 사이트가 열려 있는 동안 5분마다 알림 걸린 아이템만 id 지정 경량 쿼리로 확인, 도달 시 브라우저 알림. 서버 없는 제약의 정직한 설계 — "탭 닫으면 멈춤" 명시, 권한은 저장 클릭에서 1회 요청, 원샷 발동(✅, 재저장 시 재무장)으로 반복 발사 방지. 알림 0개면 요청도 0
- [x] **내 데이터** (푸터): localStorage 6종(즐겨찾기/진행 퀘스트/체크리스트/은신처/레벨/알림) JSON 내보내기·가져오기·전체 초기화. 가져오기는 app 식별자 + 화이트리스트 키만 복원(이물질 차단 E2E 확인) 후 reload
- [x] 4x 스로틀 최대 롱태스크 445ms — 기준 통과
- 보류 판단: 주간 리포트 시각화(텍스트 요약 데이터라 효과 대비 모호), 영어 지원(전 UI 문자열 — 별도 페이즈 필요)

### Phase 19 — 은신처 FIR 구분 + 퀘스트 아이템 "정크박스" 그리드 (완료)
- [x] 은신처 FIR: 1.0부터 은신처 건설 요구 일부도 레이드 획득(FIR)만 인정 — API `itemRequirements.attributes(foundInRaid)` 실측(317개 중 104개 FIR) 후 은신처 뷰 뱃지 + 통합 체크리스트 집계/필터/칩(퀘 FIR·퀘스트·은신처 FIR·은신처 분리)에 반영
- [x] 퀘스트 아이템 그리드 (준비물→퀘스트의 보기 전환, 퀘스트별 목록은 유지): 계정 전체/트레이더별 필요 아이템을 인벤토리식 타일 그리드로 — 아이콘 + ×수량 바 + FIR 뱃지 + 퀘스트 툴팁. **클릭 순환: 미확보 → 모아둠(골드 빗금) → 완료(접힘 섹션) → 해제** (`tc:quest-item-marks`, "내 데이터" 백업 포함). 준비물 +/− 개수와는 독립적인 "한눈 분류" 상태
- [x] 타일 228종 — 2단계 렌더(60→전체), 4x 최대 롱태스크 689ms 통과, 모바일 타일 112px 터치 크기·오버플로 0

### Phase 20 — 저장 신뢰성: "로그인 없이" 결론 (완료)
- 분석 결론 (저장소 주인 질문 "로그인 만들어야 하나"): **만들지 않는다.** localStorage는 껐다 켜도 유지됨 — 사라지는 경우는 데이터 삭제/시크릿/다른 기기뿐. 서버 없는 로그인은 성립 불가, 무료 BaaS(Firebase 등)는 한도·키 관리·개인정보 책임·서비스 종속을 1인 무료 프로젝트에 떠넘김. 기기 간 이동은 Phase 18 파일 백업으로 충분
- [x] 대신 저장 신뢰성 보강: ① `navigator.storage.persist()` 영구 보관 요청(앱 시작 + 다이얼로그 열 때) — 저장 공간 자동 정리에서 제외, ② 내 데이터 다이얼로그에 "자동 저장됨" 안내 + 항목별 저장 개수 + 영구 보관 승인 상태(⚠ 미승인 시 백업 권장) + 마지막 백업 N일 전(`tc:last-export` 메타, 백업 파일에는 미포함)

### Phase 21 — 1.0 스토리라인 구분 + 은신처 건설 순서 (완료)
- [x] **퀘스트 스토리라인/사이드 구분**: 1.0의 메인 스토리는 트레이더 의뢰와 별개인 "스토리 챕터" 10개 — **tarkov.dev API에는 없음**(tasks 510개 전부 트레이더 의뢰 = 사이드퀘스트, 2026-06-12 실측). EFT 위키 `Category:Story chapters`를 `scripts/fetch-storyline.mjs`(저작 도구)로 파싱해 `public/data/storyline.json` 생성 (CC BY-SA 출처 표기)
  - 퀘스트 탭 모드 세그먼트 [사이드퀘스트(트레이더 의뢰) | 스토리라인]. 챕터 카드: 진행 순서·시작 조건·설명(비공식 번역, 스크립트 CURATED에서 관리)·목표 전체(영어 원문, 분기/선택 구조 보존)·완료 체크(`tc:story-done`, 백업 포함)·위키 링크. 최종장 "티켓"은 엔딩 분기 뱃지
  - 챕터 순서는 시작 조건이 풀리는 진행 순서로 큐레이션: 투어(자동) → 추락하는 하늘(투어 중 파생) → 필드 발견형 4종 → 미궁 → 언허드 → 보레아스(1.0.5 신규) → 티켓(최종장·엔딩 4종)
- [x] **은신처 건설 순서**: 선행 조건(같은 스테이션 아래 레벨 + `stationLevelRequirements`)을 위상 정렬(`src/lib/hideoutOrder.ts`) — 68레벨 전부 선행 위반 0 실측. 동률은 (트레이더 LL 게이트→스킬→레벨→이름) 순, LL 게이트는 선행에서 물려받음(선행이 LL3이면 자신도 사실상 LL3 이후)
  - 은신처 뷰 모드 세그먼트 [스테이션 | 건설 순서]. 스텝마다 요구 아이템이 순서대로 나와 "다음에 모아야 할 FIR"이 바로 보임. "건설 가능"(선행 완비) 골드 표시, FIR 필요 레벨만/건설한 레벨 숨기기 필터, "지었음" 체크는 기존 연쇄 로직 공유
- [x] 4x 스로틀 실측: 최대 롱태스크 677ms — 1초 기준 통과 (건설 순서는 12→전체 2단계 렌더). 모바일 375px 오버플로 0. profile-ui.mjs에 시나리오 4개 추가

### Phase 22 — "영어 위키 대신 우리 사이트": 스토리·퀘스트 공략 한국어 상세화 (완료)
- 목적: 한국 유저가 영어 위키 번역의 불편 없이 이 사이트만으로 퀘스트를 진행할 수 있게 — 위키 원본급 상세도
- [x] **챕터 목표 전량 한국어화**: 435줄 수동 번역 사전 `scripts/storyline-objectives-ko.json` (영어 원문이 키) — `fetch-storyline.mjs`가 storyline.json에 병합, 누락분은 영어 폴백 + 경고 로그. 용어는 tarkov.dev ko와 통일 (프라퍼·래그맨·등대지기·세관·삼림·쇄빙선…)
- [x] **스토리 챕터 공략**: 위키 Guide 섹션(10챕터 ~145KB, 하위섹션 178개)을 GitHub Models로 **요약이 아닌 충실 번역** → `public/data/story-guides/{slug}.json`. 섹션 구조·표(필요 아이템)·위치 스크린샷 유지 — 이미지는 위키 CDN 640px 썸네일 핫링크(`scripts/wiki-images.mjs`, 저장소에 미수록, 출처 표기)
  - 워크플로 `story-guides.yml`은 **dispatch 전용** (10챕터 백필은 1~2회 실행으로 끝 — 일일 무료 50회를 quest-guides 30·브리핑 5와 나눠 쓰므로 cron 안 걺). 위키 대격변 시 redo 입력으로 재생성
  - 무료 한도 함정 실측: 일일 한도와 별개로 **분당 제한 429**가 연속 호출에서 발생 → 폴백 모델 + 40초 대기 재시도 + 호출 간 5초
  - UI: 스토리라인 챕터 카드 → 상세 화면 (목표 한국어 체크리스트 + 섹션별 공략 본문/이미지/라이트박스). 보레아스 51섹션 대비 8→전체 2단계 렌더
- [x] **일반 퀘스트 가이드 v2**: 프롬프트를 "2~8단계 요약" → "2~12단계 충실 번역 + 아이템 입수처(플리/트레이더/맵 위치) 필수 포함"으로 격상, 위치 스크린샷 `images` 추가(최대 12장). `version: 2` 필드로 구분 — 매일 30개씩 신규 우선, v1 재생성은 그다음 (전체 ~17일). 퀘스트 상세에 이미지 스트립 + 라이트박스
- 번역 품질 책임: 목표(수동)는 정확, 공략(AI)은 "AI 번역 — 원문 확인 권장" 명시. 챕터명 비공식 번역 명시 유지

### 전면 개편 로드맵 — Phase 23~27 (친구와 합의)

> 원래 "Phase 14~18"로 계획했으나 그 번호는 이미 다른 작업에 쓰여서 23~27로 옮겨 기록.
> 일부 항목은 계획 수립 후 Phase 19/21에서 먼저 구현됨 — 중복 구현 금지, 남은 것만 진행.

- **23: 내비 그룹화 + "해금 퀘스트" 페이지** (아래 Phase 23 참고)
- **24: 퀘스트 FIR 트래커** — ✅ Phase 24에서 완료 (하이드아웃 트래커까지 묶어서)
- **25: 하이드아웃 FIR 트래커** — ✅ Phase 24에 흡수 완료 (의존성 조직도 + 정크박스 차감)
- **26: 맵 퀘스트 플래너 1단계** — ✅ Phase 25에서 완료 (멀티 선택 + 유형 분류 + 레이드 가방)
- **27: 맵 퀘스트 플래너 2단계** — ✅ Phase 26에서 완료 (SVG 수록 + 런타임 마커 오버레이, 조사 권장안 그대로)

### Phase 23 — 전면 개편 1단계: 내비 그룹화 + "해금 퀘스트" 페이지 (완료)

- [x] **내비 그룹화** (기능 삭제 없이 재배치): 탭 11개를 그룹 5개로 — [브리핑] [퀘스트 도구: 퀘스트·준비물·해금] [시세 도구: 아이템 검색·가성비·급등/급락·돈벌이·탄약] [모딩] [맵]
  - 2단 탭 구조 (데스크톱·모바일 공통): 상단 그룹 탭(슬라이딩 인디케이터 유지) + 하단 서브 탭(그룹에 탭이 2개 이상일 때만 표시). 드롭다운 대신 2단 탭 채택 — 매거진 문법(밑줄 탭)과 일관, 모바일에서도 같은 구조라 분기 없음
  - 그룹 재진입 시 마지막에 보던 서브 탭 복원(세션 메모리). localStorage 키·기존 상태 변경 없음 (활성 탭은 원래 비영속)
  - profile-ui.mjs 탭 클릭 헬퍼를 그룹→서브 2단계로 갱신
- [x] **"해금" 페이지** (퀘스트 도구 그룹): tasks `finishRewards.offerUnlock` 실측(510개 중 166개 퀘스트, 228개 해금 오퍼) → "아이템 → 해금 퀘스트(트레이더 LL·요구 레벨)" 역인덱스
  - 기존 3MB 퀘스트 캐시에 offerUnlock 필드만 추가(추가 요청 0), 퀘스트 탭과 세션 캐시 공유
  - 아이템 검색(한/영) → 카드 → 해금 오퍼(트레이더·LL) + 요구 플레이어 레벨 + **선행 퀘스트 체인 전체를 진행 순서대로**(DFS 후위 = 위상 정렬). 각 퀘스트 클릭 시 기존 퀘스트 상세로 이동(`setPendingQuest` 재사용)
  - 해금 아이템이 아닌 검색어는 전체 아이템 캐시(1.3MB, lazy)와 대조해 "퀘스트 해금 불필요 — 트레이더 기본 오퍼/플리 구매 가능" 안내
  - `scripts/check-unlocks.mjs`: 전체 228개 오퍼 체인의 위상 정렬 위반 0 자동 검증 + 인기 해금 아이템(Labs 키카드 Black·THICC 케이스·REAP-IR·GPU·7N40) 체인을 위키 원문과 대조 — 4/4 언급 확인. 건스미스 1→25가 순서대로 나오는 것으로 체인 정렬 교차 검증
  - API 함정 실측 2건: ① 같은 태스크 안에 동일 offerUnlock이 중복으로 오는 케이스 5건 → id|트레이더|LL 키로 중복 제거, ② 동명 퀘스트(진영/루트 변형, "The Price of Independence" 등 8쌍)는 별개 id의 실존 퀘스트라 유지 — 카드 메타 표기만 중복 제거
- [x] 4x 스로틀 실측: 최대 롱태스크 800ms — 1초 기준 통과 (해금 탭 진입 839ms·체인 rAF 7ms·퀘스트 상세 점프 정상). 모바일 375px 오버플로 0 (브리핑/해금 그리드/해금 상세). profile-ui.mjs에 그룹 내비 클릭 헬퍼 + 해금 시나리오 3개 추가

### Phase 24 — FIR 트래커: 친구 스케치 "갭 메우기" (완료)

- 갭 분석 (스케치 7개 요구 대비): 충족 0 · 부분충족 4(구분·상인별 목록·그리드·스테이션 패널은 있으나 좌/우 동시·차감 없음) · 미충족 3(양방향 차감, 은신처 그리드, 조직도 트리) → **새 "FIR 트래커" 탭**(퀘스트 도구 그룹)으로 갭만 구현, Phase 19/21 재구현 없음
- **준비물 탭과의 관계**: 준비물 = 통합 요약 / 트래커 = 상인·스테이션별 상세 뷰. 보유 개수는 `tc:prep-counts` **하나만** 공유(이중 관리 금지) — 어느 쪽에서 바꿔도 같이 움직임. "지었음"도 기존 `tc:hideout-built` + 연쇄 로직 공유(`cascadeBuilt`로 추출). **새 localStorage 키 0개** → 내 데이터 백업 변경 불필요
- [x] **퀘스트 트래커**: 상인 초상화 선택 → 좌측 퀘스트 목록(레벨 오름차순, FIR만 기본) / 우측 그 상인 정크박스 그리드(우하단 남은 수량 뱃지, 보유 시 좌상단 −1 버튼). 좌측 아이템 클릭 +1 ↔ 우측 실시간 차감, 우측 타일 클릭도 +1 (양방향). 상인별 남은 수량은 보유분 전부를 그 상인에 쓴다고 가정한 표시(보유가 계정 전체 공유라서 — 힌트 명시)
  - 스토리/사이드 구분: **storyline.json(Phase 21 수동 데이터) 택1** — 카파 필수 여부는 "수집가" 사이드 라인 표식이라 스토리 구분이 아님. 상인 줄 끝 "스토리라인" 선택 시 챕터를 진행 순서로 표시하되, **챕터 제출 아이템은 API에 없어 정크박스 집계 비대상임을 명시** (완료 체크는 퀘스트 탭과 공유 표시)
- [x] **은신처 트래커**: 좌측 의존성 **조직도 트리**(위→아래 7층) + SVG 연결선 — 평소 흐리게, 선택 스테이션과 닿은 선만 골드. 스테이션 클릭 → 레벨별 아이템 패널(클릭 +1·지었음 토글) → 우측 "은신처 전체" 정크박스 차감. 지은 레벨 몫은 집계 자동 제외
  - **층 계산 함정**: 스테이션 단위 그래프엔 순환이 있음(발전기↔환기 실측) → 층은 1레벨의 **레벨 단위 DAG 깊이**로 계산 (Phase 21 위상 정렬 위반 0이 근거). 간선 ~65개는 전부 그리되 강조로 가독성 확보. 연결선은 노드 rect 실측(컨테이너 기준 좌표) — 줄바꿈·리사이즈에 자동 적응
- [x] 4x 스로틀 실측: 최대 롱태스크 721ms — 1초 기준 통과 (트래커 차감 반영 ~250ms, 좌측 목록·그리드 모두 2단계 렌더). 모바일 375px 오버플로 0, 좌/우는 860px 이하에서 상/하 스택. profile-ui.mjs 시나리오 5개 추가

### Phase 25 — 맵 퀘스트 플래너 1단계: "한 레이드에 몰아 밀기" (완료)

- [x] **"플래너" 탭** (퀘스트 도구 그룹): 맵 칩(목표 단위 `objectives.maps` 기준 — 태스크 단위 map보다 정확, 야간 공장·그라운드 제로 21+ 같은 변형 맵도 분리 집계) → 그 맵에 목표가 있는 퀘스트 체크 목록 (한/영 검색·내 레벨·★ 진행 중 필터, 상세→ 링크는 기존 퀘스트 상세로)
- [x] **퀘스트 멀티 선택**: `tc:planner-picks`(mapId→questId[], 새 localStorage 키 — 내 데이터 백업 화이트리스트 추가). 가방·브리핑은 필터와 무관하게 체크된 것 전부 기준 (레이드 중 폰 확인 시나리오)
- [x] **목표 유형 분류**: objective type 19종 실측(1,510개) → 6분류 매핑 — 📍마커 설치(mark) / 🧰아이템 설치·사용(plantItem·useItem) / 👣발견·방문·회수(visit·findQuestItem·findItem) / 📥아이템 숨기기(plantQuestItem) / 💀처치(shoot) / 📋기타(extract 등). 선택 후 "작전 브리핑"에 유형별 그룹 표시
- [x] **레이드 가방 패널**: 지참물 합산(마커는 mark 목표당 1개, 설치물 ×count, useAny는 첫 항목+'류'), 숨길 퀘스트 아이템, 처치 요약(대상별 킬 합산), 방문·기타 카운트 + tarkov.dev 맵 딥링크 + 선택 초기화. 데스크톱 우측 sticky / **모바일 상단 고정**(접기 가능 details, 탭바 높이 83px 실측 맞춤) — 퀘스트 쿼리에 목표 단위 maps·markerItem·questItem·targetNames·useAny 추가 (캐시 공유, 추가 요청 0)
- [x] **Phase 26 타당성 조사** (`docs/map-planner-research.md`): 좌표 실측 — zones 461·possibleLocations 113개 목표, 게임 월드 좌표계, 맵별 커버리지 44~83%(Icebreaker 0%). 이미지 — the-hideout maps.json(MIT)에 transform·rotation·bounds 완비, SVG는 CC BY-NC-SA 4.0(비상업·출처표기·안티치트 조항, 우리 무료 팬사이트는 충족). **판정: 자체 렌더 가능** — SVG 수록+마커는 런타임 오버레이(SA 파생물 논점 회피), 좌표 없는 목표는 목록 폴백, 딥링크는 폴백으로 흡수
- [x] 4x 스로틀 실측: 최대 롱태스크 676ms — 1초 기준 통과 (플래너 진입 385ms·맵 선택 735ms·체크 반영 232ms). 모바일 375px 오버플로 0, 가방 sticky 탭바와 틈 1px 실측

### Phase 26 — 맵 퀘스트 플래너 2단계: 맵 위 마커 렌더 (완료)

- [x] **라이선스 (양보 불가 조건 이행)**: 맵 SVG 10종을 `public/maps/`에 수록 + `LICENSE.md`(출처: tarkov-dev-svg-maps, CC BY-NC-SA 4.0 / 메타: maps.json, MIT) + README 데이터 출처·면책 갱신. **비상업 유지 선언 명시**. 마커는 런타임 오버레이로만 — SVG 파일은 svgo 무손실 최적화 외 수정 없음 (SA 파생물 논점 회피). 수집은 `scripts/fetch-map-assets.mjs` (대격변 시 재실행)
- [x] **좌표 변환**: `lib/mapProject.ts` — tarkov-dev leaflet CRS 실측 이식: (gx,gz)를 coordinateRotation으로 회전 → px = t0·rx + t1, py = **−t2**·ry + t3. **함정 2개 실측**: ① bounds/svgBounds는 [x,z] 쌍 (tarkov-dev getBounds가 lat/lng로 뒤집어 씀), ② Reserve만 svgBounds 별도. 검증: `scripts/check-map-projection.mjs` — 전 좌표 986개 투영, 경계 밖 **0개**
- [x] **맵 뷰어** (`MapViewer.tsx`): 플래너 "맵 보기" 토글 → SVG(토글 시에만 lazy fetch + 세션 캐시) + 선택 퀘스트 마커(6분류 아이콘 + 퀘스트별 8색 순환, 목록 체크박스 옆 색 점과 매칭). 팬·휠 줌·핀치·더블클릭(탭) — **단일 레이어 CSS transform만 갱신(React 리렌더 0), 마커는 --inv 역스케일로 화면 크기 고정**. 층 정의 맵은 svgLayer 그룹 display 토글. 마커 클릭 → 퀘스트명+목표 팝오버 (드래그·줌 시 닫힘)
  - **함정 실측**: wrap의 setPointerCapture가 마커 버튼 click을 삼킴 → 마커·팝오버·핏 버튼 위에서는 팬 캡처 시작 안 함
- [x] **커버리지 처리**: 좌표 없는 목표는 뷰어 아래 "좌표 미제공 목표 N개" 접이식 목록(조용히 숨기지 않음). SVG 없는 맵(쇄빙선·연구소·미궁·신맵)은 안내 + 가방 패널 tarkov.dev 딥링크
- [x] **스폿체크** (대표 3맵, 위키 설명과 대조): 세관 — BP Depot 연료탱크 4개가 맵 전역 분산 + 기숙사 목표 2개가 기숙사 건물에 군집, "배경 조사" 회중시계 마커 = 탱크 위치 ✓ / 공장 — Farming 컨트롤 보드 상단 2 + Black Swan 열교환기 3 분산 ✓ / 해안선 — Spa Tour 헬기·Cargo X가 리조트에 군집 ✓
- [x] 4x 스로틀: SVG 로드 724ms·줌 20회 연사 rAF 2ms(transform 전용 확인)·최대 롱태스크 766ms — 기준 통과. 모바일 375px 오버플로 0, 마커 터치 타깃 40px 실측, touch-action:none + 더블탭 폴백

### Phase 27 — 브리핑 자동화 안정화: schedule 누락 대응 (완료)

- [x] **schedule 트리거는 신뢰 불가**: GitHub Actions의 `schedule`은 공식적으로 지연·누락이 보장되지 않는 best-effort 동작 — 2026-06-12·06-13 연속으로 daily-briefing의 schedule 이벤트가 **0건**(dispatch만 존재) 발생. 정각(0분) 회피만으로는 부족, **누락 자체를 따라잡는 구조**가 필요
- [x] **cron 다중화 (멱등 백업)**: `daily-briefing.yml`에 시각 분산 cron 3개(09:17·10:32·11:47 KST), `weekly-report.yml`·`quest-guides.yml`에 백업 1개씩. 아침에 하나라도 성공하면 그날 분은 완료. **함정: 생성 스크립트에는 "오늘 파일 있으면 생략" 로직이 없고 매번 `generatedAt`을 새로 찍어 무조건 덮어쓴다** → 워크플로우 맨 앞에 `guard` 스텝(오늘 KST 파일 존재 시 이후 단계 전부 skip) 추가해 멱등화 (없으면 백업 cron이 아침 브리핑을 다시 AI 호출·덮어쓰기·재배포). 수동 dispatch는 강제 재생성이라 guard 면제. quest-guides는 백로그 소진형이라 두 번 돌아도 다음 30개만 진행(중복 없음) → guard 불필요
- [x] **누락 가시화**: 브리핑 탭 — 최신 일일 브리핑 날짜가 KST 오늘이 아니면 상단에 "오늘 브리핑이 아직 생성되지 않았습니다 — 자동 갱신 대기 중" 안내(`.briefing-stale`, warn 톤). 백업 cron이 오전 중 따라잡으면 자동으로 사라짐 — 방문자가 빈 화면으로 오해하지 않게
- [ ] 검증: schedule이 실제로 도는지는 cron 등록 후 시간이 걸려 다음 날 아침 결과로 확인 (푸시만 선행)

### Phase 28 — FIR 통합 운영 페이지 (완료)

친구 최종 스케치: 흩어져 있던 `준비물`·`FIR 트래커` 두 탭을 **하나의 `FIR` 탭**으로 통합. 좌우 2분할 운영 화면이 기본이고, 기존 화면은 **기능 삭제 없이 보조 보기로 흡수**.

- [x] **탭 통합**: `prep`·`tracker` 두 탭을 제거하고 `fir` 탭 하나로(「퀘스트 도구」 그룹). `FirTab`이 보기 전환 — 통합 운영(기본) / 준비물 목록(`PrepTab`) / 트래커·조직도(`TrackerTab`). 두 옛 컴포넌트를 그대로 호스팅해 검색·필터·은신처 의존성 조직도·상인별 정크박스가 모두 보존됨
- [x] **2분할 운영(`FirOps`)**: 좌 = 소스 패널([퀘스트]/[하이드아웃]). **퀘스트는 상인 가로 선택(초상화+이름) → 그 상인의 퀘스트가 레벨 오름차순 아코디언으로**, 각 퀘스트 행에 필요 FIR 아이콘(×수량) + "클리어함" 버튼. 하이드아웃은 스테이션 행 + "건축 완료" 버튼. 완료 행은 **어둡게 + 45° 빗금 오버레이**. 우 = 남은 FIR 아이템을 분류별 정크박스 그리드(우하단 남은 수량 뱃지). **좌측에서 완료를 누르면 그 수요가 우측에서 실시간 차감** — 퀘스트=`tc:done-quests`(신규), 하이드아웃=기존 `cascadeBuilt`/`tc:hideout-built`
- [x] **수량 = 스테퍼(클릭 누적 폐기)**: 우측 각 타일에 `− 보유 +` 스테퍼로 부분 수집량 직접 증감(0에서 −, 필요수에서 + 비활성). 기존 "타일 클릭 +1 누적"은 운영 화면에서 제거 — 저장소는 `tc:prep-counts` 그대로 공유(준비물 목록과 양방향 동기화)
- [x] **정렬 흔들림 버그 수정(최우선)**: 기존엔 "남은 수량 순" 동적 정렬이라 스테퍼로 +/− 할 때마다 타일이 자리를 옮겼음. **정렬 키를 보유(counts)와 완전히 분리** — `orderRef`(id→인덱스)에 처음 본 아이템만 `need` 내림차순으로 1회 인덱스를 부여하고 이후 고정. `ordered` 메모 deps는 `[demand]`뿐이라 보유 변경엔 불변 → 스테퍼 조작에도 위치 유지(완료 항목은 제자리에서 흐려지기만). 프로파일러에 순서 불변 동작 검증 추가(7.12 OK)
- [x] **분류 매핑(`lib/storageBuckets.ts` + `api/itemTypes.ts`)**: tarkov.dev `Item.types` 실측(2026-06-13, 18/18 대표 아이템 정확) 기반 우선순위 매핑. **함정 2가지**: ① `wearable`는 가방·리그·총에 다 붙어 분류 불가 → 제외, ② Salewa·Propital은 `[meds,provisions]`라 의료를 음식보다 먼저 둬야 약이 음료로 안 샘. 세부 7버킷을 두되 화면 분류 탭은 친구 최종 5분류 **장비·정크박스·음식·의료·기타**로 접음(`displayGroupOf` — 의료는 독립, 열쇠·무기·부품은 기타). 라이브 데이터 끝단 검증 211종 분포 정상(장비/정크박스/음식/기타+의료)
- [x] **퀘스트 완료 저장소 신설**: `tc:done-quests`(`DONE_QUESTS_KEY`) — 기존엔 "완료" 개념이 없었음(`active-quests`는 ★진행 중 필터로 반대 개념). 완료=그 퀘스트만(선행 체인 자동 완료는 의도치 않은 일괄 처리 위험이라 배제)
- [x] **성능(CLAUDE.md 기준)**: 아이콘+스테퍼 타일 그리드는 2단계 렌더(`FIRST_PAINT=36` → 분류 전환마다 리셋). CPU 4x 스로틀 실측 — FIR 조작 전부 rAF 응답 ≤8ms, 단일 롱태스크 최대 765ms(<1s). 좌우 분할은 `@media(max-width:760px)`에서 상하 스택, 그리드는 560px에서 열 축소
- 메모: 이 페이지는 세션 중 거론된 "보관함 단독 뷰" 안을 대체함(같은 매핑 lib·정크박스 시각 재사용). 탭 번호는 브리핑 안정화가 이미 Phase 27을 써서 28로 매김(친구는 Phase 27로 지칭). 좌측 하이드아웃 "건축 완료"는 스테이션 전체 일괄 토글이며, 레벨별 부분 건축은 "트래커·조직도" 보조 보기의 조직도에서 정밀 처리

### Phase 29 — 맵 마커 팝오버 "필요 열쇠" (완료)

친구 피드백: 맵 마커를 눌렀을 때 그 목표가 잠긴 방/구역이면 어떤 열쇠가 필요한지 보이게.

- [x] **데이터 — `requiredKeys` 실측**: `TaskObjective` 인터페이스엔 없고 구체 타입 fragment(Item/Mark/QuestItem/Basic/Extract)에 있음. 구조는 `[[Item]]` — **그룹 배열**: 그룹 간 **AND**(모두 필요), 그룹 내 **OR**(아무거나). 실측(82개 목표): 대부분 단일 그룹·단일 키, "219호 또는 220호"(그룹 내 복수=OR)·"KIBA 바깥문 + 안쪽문"(그룹 복수=AND) 사례 존재. 기존 quests 쿼리의 4개 fragment(Item/Mark/QuestItem/Basic)에 필드만 추가해 받음(ko=아이콘+한국어, en=영어명 색인). 추가 네트워크 요청 없음
- [x] **UI — 팝오버 열쇠 줄(`MapViewer`)**: 퀘스트명+목표설명 아래 "🔑 필요 열쇠" 줄. 열쇠 아이콘(iconLink)+한/영 이름, 그룹 내는 "또는", 그룹 간은 "+"(둘 다 필요). 열쇠 불필요 목표는 줄 자체 숨김. `ViewMarker.keys`는 `MarkerKey[][]`로 표시명·검색어를 미리 가공해 전달 — MapViewer가 quests API를 모르게 유지
- [x] **열쇠 클릭 → 아이템 검색**: `onItem` 콜백을 App→PlannerTab→MapViewer로 배선(티커 클릭과 같은 `pickFromTicker`). 검색어는 열쇠의 한국어명(미번역이면 영어명 그대로라 검색도 매칭). 시세·구매처 바로 확인
- [x] **표시 정확도(위키 대조)**: 동료들 파트2→Shoreline cottage back door key, 스파 관광 파트4→219호 또는 220호, Provocation→KIBA 바깥문+안쪽문 — 모두 일치. 미번역 열쇠는 영어명만 표시(정상)
- 메모: 마커는 좌표(zones/possibleLocations) 보유 목표에만 뜨므로, 열쇠 줄도 "좌표 있는 잠긴 목표"에서만 보임 — 친구 요청 범위(마커 팝오버)와 정확히 일치

### Phase 30 — FIR 페이지 다듬기 + 중복 퀘스트 버그 (완료)

Phase 28 FIR 통합 페이지 위에 얹는 작업 (친구는 "Phase 29"로 지칭 — 29는 이미 맵 열쇠가 씀).

- [x] **버그(최우선) — 중복 퀘스트**: 래그맨 "New Beginning"이 좌측에 3중 표시 + 우측 FIR **3배 과다 집계**. 원인은 **API가 같은 (영어명+상인) 퀘스트를 여러 id로 반환**(게임모드·이벤트 변형) — 실측 510개 중 8그룹 11건 중복("New Beginning" 4 id, "Make Amends" 3 id, "Drip-Out/Textile" 등). id 기준 dedup은 불가(전부 다른 id)라 **`mergeTasks`에서 `(영어명+상인)` 기준 전역 dedup** 신설(`dedupeTasks`): 대표는 **목표 수가 가장 많은 것**(New Beginning은 14목표=FIR 피규어 10종 보유 변형이 선정됨을 실측 확인), `requires`는 대표 id로 재매핑하고 `unlocks`는 대표 집합에서 재계산. 전역 수정이라 퀘스트 탭·준비물·플래너의 중복/과다집계도 동시 해결
- [x] **하이드아웃 레벨별 상세**: 하이드아웃 탭 스테이션 행을 펼치면(`▸/▾`) 레벨별 요구 아이템을 "N레벨 — 아이콘+한/영+×수량" 으로 표시(`levels[].itemRequirements`, 화폐 제외). **이미 지은 레벨은 어둡게+빗금**(기존 `tc:hideout-built` 연동), FIR 요구는 배지
- [x] **퀘스트 아이템 체크 연동**: 좌측 퀘스트 행의 필요 FIR 아이템마다 `− 보유 +` 스테퍼(P27 우측 그리드와 같은 방식). `tc:prep-counts` 공유라 **좌측에서 바꾸면 우측 정크박스 그리드도 실시간 반영**(반대도). 필요수 충족 항목은 취소선·흐리게
- [x] **성능(CLAUDE.md)**: 스테퍼가 붙어 무거워진 좌측 아코디언에 **상인 전환 2단계 렌더**(`QUEST_FIRST_PAINT=8`) 추가. CPU 4x 스로틀 실측 — 무거운 상인(래그맨) 전환 90ms/rAF 2ms, 스테이션 펼침·스테퍼 등 FIR 조작 전부 rAF ≤7ms(<1s). 모바일은 아이템 행이 한 줄씩(이름 말줄임)이라 스테퍼 정렬 유지

## 6. 환경 역할 분담

| 작업 | 환경 |
|---|---|
| 설계·문서·리서치 | Cowork |
| 대시보드 코딩 (Phase 1) | **Claude Code 권장** (Cowork도 가능) |
| 브라우저 실제 테스트 | Cowork (Chrome 연동) |
| 브리핑 자동화 (Phase 2) | **GitHub Actions** (전자동, 사람 개입 불필요) |
| git push / 배포 | 노트북 터미널 (Code 사용 시 Code가 대행) |

## 7. 알려진 제약

- Cowork 샌드박스에서 api.tarkov.dev 직접 호출 불가(네트워크 허용목록) → API 테스트는 브라우저(Chrome 연동) 또는 노트북에서 수행
- 게임 버전: 1.0.5.0 Icebreaker (2026-06) 기준. 패치마다 아이템/메타 변동 가능
